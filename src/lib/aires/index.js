/**
 * Aires - Enhanced bot functionality
 * Provides user profile management, history tracking, and service detection
 */

const userProfileManager = require('./userProfileManager');
const templateManager = require('./templateManager');

module.exports = {
  userProfileManager,
  templateManager
};
